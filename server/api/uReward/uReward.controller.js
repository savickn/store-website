/**
 * Using Rails-like standard naming convention for endpoints.
 * GET     /y              ->  index
 */

'use strict';

// Gets a list of URewards
export function index(req, res) {
  res.json([]);
}
