'use strict';

describe("Service: User", function() {
  beforeAll(module('passportApp'))

  var UserService,
      Auth;

  beforeAll(inject(function(_UserService_, _Auth_) {
    UserService = _UserService_;
    Auth = _Auth_;
  }))

  it('not sure', function() {

  })

})
