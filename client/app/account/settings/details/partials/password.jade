
form.form(name='form' ng-submit='changePassword(form)' novalidate)
  .form-group
    label Current Password
    input.form-control(type='password' name='oldPassword' placeholder="Current password" ng-model='user.oldPassword' mongoose-error)
    p.help-block(ng-show='form.password.$error.mongoose')
      | {{ errors.other }}
  .form-group
    label New Password
    input.form-control(type='password' name='newPassword' placeholder="New password" ng-model='user.newPassword' ng-minlength='3', required)
    p.help-block(ng-show='(form.newPassword.$error.minlength || form.newPassword.$error.required) && (form.newPassword.$dirty || submitted)')
      | Password must be at least 3 characters.
  .form-group
    label Confirm Password
    input.form-control(type='password', name='confirmPassword' placeholder="Confirm new password" ng-model='user.confirmPassword' ng-minlength='3' required)
    p.help-block(ng-show='(form.newPassword.$error.minlength || form.newPassword.$error.required) && (form.newPassword.$dirty || submitted)')
      | Password must be at least 3 characters.

  p.help-block  {{ message }}

  button.btn.btn-lg.btn-primary(type='submit') Save changes
