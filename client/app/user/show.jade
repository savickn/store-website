nav.navbar.navbar-default
	ul.nav.navbar-nav
		li(ng-click="status = 'info'") Account Details
		li(ng-click="status = 'rewards'") Rewards
		li(ng-click="status = 'orders'") Orders


		//li
			a(href="/products/featured") Featured 

.user-info(ng-show="status === 'details'") Details
.user-rewards(ng-show="status === 'rewards'") Rewards
.user-history(ng-show="status === 'orders'") Orders 

		span.text-muted {{user.email}}
		a.trash(ng-click='delete(user)')
			span.glyphicon.glyphicon-trash.pull-right
